# Neo Team Workspace Package

Neo-native team package using mapped role names (no Morpheus-team specialist names).

Includes root files plus per-agent `AGENTS.md`, `SOUL.md`, `USER.md`, `IDENTITY.md`.
